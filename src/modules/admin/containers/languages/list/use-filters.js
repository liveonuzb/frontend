// Languages list does not use server-side filtering or pagination.
// This file exists to keep the list/ folder structure consistent.
// The languages page uses DnD-based reordering instead of filters.
export const useLanguageFilters = () => null;
